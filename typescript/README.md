# Usage

For the single `array` concept from array.ts:
`npm run build -- array`
`npm run dev -- array`
`npm run run -- array`
`npm run test -- functions`

## Testing

Vitest (pronounced as "veetest")
